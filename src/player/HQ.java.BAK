package player;
import battlecode.common.*;

public strictfp class HQ {

	public static void HQ(RobotController rc, State state) throws GameActionException {
		Direction[] directions = state.getDirections();
		for(State.Direction dir : )
			tryBuild(RobotType.MINER, dir);
	}
}
